{
  "name": "connect-four-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "bun@1.1.44",
  "scripts": {
    "dev": "pnpm --filter next-connect-four dev",
    "build": "pnpm --filter next-connect-four build",
    "start": "pnpm --filter next-connect-four start",
    "lint": "pnpm --filter next-connect-four lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "tsx": "^4.19.1"
  }
}
